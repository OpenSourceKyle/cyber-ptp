```bash

# Google Dorks
filetype:pdf <KEYWORD>
filetype:pdf site:<URL>
cache:<URL>
site:<URL> "President and CEO at Agiliance"
ip:<IP>
site:*.<URL> -www

# theHarvester
mkdir -p ~/.theHarvester && \
echo -e "apikeys:\n  hunterio:\n    key: 50f3f694774de786965dadaaf5687021c2414618\n  dnsdumpster:\n    key: 6e830788a7c1a520ecca16fed503d0c10bb68d647020e07b915f5801f5e18b8e" > ~/.theHarvester/api-keys.yaml

theHarvester --limit 100 --source crtsh,rapiddns,dnsdumpster,bufferoverun,urlscan,hackertarget,certspotter,otx,hunter --domain elearnsecurity.com

# nslookup
nslookup <TARGET>
nslookup -type=PTR <IP_ADDR>
nslookup -type=MX <TARGET>
nslookup -type=NS <TARGET>

# dig
dig <TARGET>

dig -x <IP_ADDR>  # IP -> DNS
dig MX <TARGET>
dig NS <TARGET>

dig axfr @<DNS_SERVER> <TARGET>

dig @<DNS_SERVER> +noedns +nocookie +norecurse <TARGET>
# EDNS breaks on Win, norecurse usu for internal networks

# fierce
fierce --domain <TARGET>

# dnsenum
dnsenum --enum <TARGET>
dnsenum --dnsserver <DNS_SERVER> --file /usr/share/wordlists/dnsmap.txt --enum <TARGET>
dnsenum --dnsserver <DNS_SERVER> --file /usr/share/wordlists/dnsmap.txt --enum --update a --recursion <TARGET>

# dnsmap
dnsmap <TARGET>
dnsmap <TARGET> -w wordlist.txt

# dnsrecon
dnsrecon --domain <TARGET>

# fping
fping --alive --elapsed --retry 0 --generate <IP_ADDR>/24

# hping3
hping3 --count 1 --icmp <IP_ADDR>
hping3 --count 1 --udp <IP_ADDR>
hping3 --count 1 --syn <IP_ADDR>
hping3 --count 1 --syn --destport 3389 <IP_ADDR>
hping3 --count 1 --interface <INTERFACE> --icmp --rand-dest 192.168.1.x

# nmap Host Discovery
nmap -sC -sV -O -T4 <TARGET>
# very noisy (top 1k ports) but effective

nmap --disable-arp-ping -PS <NETBLOCK>/24

nmap --disable-arp-ping -PE <TARGET>
nmap --disable-arp-ping -PM <TARGET>
nmap --disable-arp-ping -PP <TARGET>

nmap --disable-arp-ping -PS53 <TARGET>
nmap --disable-arp-ping -PU53 <TARGET>
nmap --disable-arp-ping -PY53 <TARGET>

# Advanced Scans
nmap -sS -p53 <NETBLOCK>
nmap -sU -p53 <NETBLOCK>
nmap -Pn -n -sS -sV \
  --max-retries 1 \
  --host-timeout 45s \
  --initial-rtt-timeout 300ms \
  --max-rtt-timeout 1000ms \
  -p 21,22,23,25,53,80,110,135,139,443,445,3389 \
  <TARGET>

sudo nmap -sU -Pn -n -p 53 --script=dns-recursion,dns-service-discovery <TARGET>
sudo nmap -sU -Pn -n -p 123 --script=ntp-info <TARGET>
sudo nmap -sU -Pn -n -p 161 --script=snmp-info <TARGET>
sudo nmap -sU -Pn -n -p 161 --script=snmp-brute <TARGET>
sudo nmap -sU -Pn -n -p 137 --script=nbstat <TARGET>
sudo nmap -sU -Pn -n -p 67 --script=dhcp-discover <TARGET>
sudo nmap -sU -Pn -n -p 69 --script=tftp-enum <TARGET>
sudo nmap -sU -Pn -n -p 1900 --script=ssdp-discover <TARGET>
sudo nmap -sU -Pn -n -p 500 --script=ike-version <TARGET>
```